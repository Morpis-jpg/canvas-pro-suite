services:
  - type: web
    name: canvas-pro-suite
    runtime: python
    plan: free
    buildCommand: "python -m py_compile server.py"
    startCommand: "python server.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
